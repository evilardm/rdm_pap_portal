export interface ApiInversion {
  id: string;
  codigo: string;
  descripcion: string;
  cerrada: boolean;
}

export interface Inversion {
  id: string;
  codigo: string;
  descripcion: string;
  cerrada: boolean;
}
